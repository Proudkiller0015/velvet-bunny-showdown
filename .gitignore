node_modules/
cache/
logs/
databases/
*.log
.env
.DS_Store

# The marathon stop signal is a local thing, not a result.
data/marathon/STOP

# Tier sim results are local (append-only, resumable); the report in docs/ is what gets committed.
data/tier-sim/
